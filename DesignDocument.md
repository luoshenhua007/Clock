# 基于 FPGA 的多功能电子表 —— 课程设计文档

## 1 课题概述与需求抽象

用 Verilog 在 FPGA（xc7a75tfgg484-2，Vivado 2024.2）上实现一台电子表，需具备：时间/日期显示与设置、3 组闹钟提醒（可解除）、倒计时，且全部操作仅用 6 个物理输入完成。

需求抽象为 4 类功能 + 1 类约束：

| 类别 | 抽象要求 |
| :-- | :-- |
| 时间 | 24h 计时（00:00~23:59），显示可选 12/24h（12h 带 A/P 指示）；时间可校准 |
| 日期 | 年月日显示，支持闰年与大小月；可设置；可切换星期视图 |
| 闹钟 | 3 组时:分闹钟，独立使能；到点提醒 5s，可解除；未解除则 5s→停 10s→再 5s 后自动解除 |
| 倒计时 | 初值可设（上限 23:59:59），运行/暂停/复位；结束提醒并回默认值 |
| 约束 | 全部功能由 4 按键 + 2 开关完成；除校时外计时不得中断 |

## 2 总体方案与系统框架

分层设计，单时钟域（50MHz），次级节拍用使能脉冲（1Hz 走时、2Hz 闪烁）。

```text
物理输入(KEY1..4, SW3/SW4)
   │
   ├─ key_debounce（消抖/短按脉冲/长按标志）
   ├─ 模式译码（SW 电平 → 组×显示/编辑）
   ▼
主控/状态机（页面、编辑字段、闹钟/倒计时控制）
   │
   ├─ rtc_counter 实时时钟/日期（闰年、大小月）
   ├─ alarm_clock 3 组闹钟 + 二次提醒 FSM
   ├─ countdown   倒计时（HH:MM:SS）
   └─ 显示选通（12/24h 换算、星期推算、格式排版、光标闪烁）
   ▼
段码动态扫描 → 8 位数码管 + LED1..4
```

## 3 状态机设计

### 3.1 顶层模式（开关电平组合）

两开关构成 4 态；**编辑中组锁存**（拨动另一开关不切换组），退出编辑后按开关档位显示。

| SW4 | SW3 | 模式 |
| :-: | :-: | :-- |
| 下 | 下 | 时间/日期 · 显示（默认，时间页） |
| 上 | 下 | 闹钟/倒计时 · 显示（默认闹钟1） |
| 下 | 上 | 时间/日期 · 编辑 |
| 上 | 上 | 闹钟/倒计时 · 编辑 |

编辑对象为进入编辑前所显示的内容；闹钟/倒计时组返回显示时保持上次所选对象。

### 3.2 按键语义（显示态/编辑态）

| 模式 | KEY1 | KEY2 | KEY3 | KEY4 |
| :-- | :-- | :-- | :-- | :-- |
| 时间/日期·显示 | 时间↔日期 | 时间:12/24h；日期:年月日↔星期 | — | 解除闹钟 |
| 闹钟/倒计时·显示 | 闹钟1/2/3 | 显示倒计时 | 闹钟:启/停用；倒计时:启/停，长按复位 | 解除闹钟 |
| 时间/日期·编辑 | 循环字段(时分秒/年月日) | + | − | 解除闹钟 |
| 闹钟/倒计时·编辑 | 循环字段 | + | − | 解除闹钟 |

KEY2/KEY3 支持长按连加/连减（编辑中字段长按期间常亮不闪）。

### 3.3 内层状态机

- **校时/编辑**：暂停走秒时修改字段；日期编辑不停表；编辑界面强制 24h/年月日，退出恢复原子视图。
- **闹钟提醒**：IDLE → 响铃 5s →（可解除）→ 静默 10s → 再响 5s → IDLE；按“时:分”分钟跳变沿触发一次，同分钟不重复。
- **倒计时**：运行（逐秒 BCD 递减）→ 暂停/继续（KEY3）→ 长按复位回初值；到 00:00:00 完成（LED 5s）并显示回默认值。

## 4 模块划分

| 模块 | 职责 | 关键点 |
| :-- | :-- | :-- |
| clk_div | 产生 1s/2Hz 节拍 | 使能脉冲，非分频时钟 |
| key_debounce | 消抖 + 短按脉冲 + 长按 | 两级同步，参数可配 |
| top_digital_clock | 模式译码/页面状态/显示选通 | 见 §3、§5 |
| rtc_counter | 时/分/秒与年/月/日计数 | 闰年(BCD 推导)、大小月、日收缩 |
| alarm_clock | 3 闹钟存储/比较/提醒 | 分跳变沿触发；解除边沿即时 |
| countdown | HH:MM:SS 倒计时 | BCD 递减、启停/复位/完成 |
| seg 驱动/扫描 | 8 位动态扫描 + 段码 | 极性、防残影（内联于顶层） |
| LED | 闹钟 1/2/3 与倒计时指示 | KEY4 仅解除闹钟 |

## 5 显示方案

- 8 位动态扫描；段码低电平点亮、位选低有效（低者点亮）；每位约 0.5ms，换位前预留熄灭段防残影。
- 布局（左→右）：时间 `HH.MM.SS _ A/P`；日期 `YYYY.MM.DD`；星期首位；闹钟 `HH.MM _ - 编号`（停用 `----`）；倒计时 `HH.MM.SS _ 圈/-`（运行=顺时针滚动圈）。
- 时间/日期换算：内部恒 24h，12h 仅显示换算并出 A/P；星期按蔡勒公式（周一=1）。

## 6 设计要点小结

1. 计时精度：仅校时暂停秒，查看/编辑日期、响铃、倒计时均不停表。
2. 键位复用：按“组×状态”上下文定义键义，减少状态爆炸。
3. 硬件适配：段码/位选极性、位序、防残影均以上板实测为准。
4. 验证：仿真按功能分 tb_part1~4，另含各模块用例；最终综合 0 错误、时序收敛并上板验收。

## 7 验收要点对照

| 题目要求 | 对应实现 |
| :-- | :-- |
| 时间显示 00:00~23:59 | 时间显示页（24h/12h 可选） |
| 日期显示 年/月/日、闰年与大小月 | 日期显示/设置 + 日历逻辑 |
| 校时（时、分） | 时间就地编辑（时/分/秒） |
| 日期设置 | 日期就地编辑（年/月/日，自动收缩日） |
| 3 闹钟 + 二次提醒 + 解除 | alarm FSM；LED 分工；KEY4 解除 |
| 倒计时设置/暂停/重来/结束提示 | countdown 模块 |
| 仅 6 个输入 | 4 键 + 2 开关电平实现全功能 |
